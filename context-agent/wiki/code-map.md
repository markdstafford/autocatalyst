# Code map

Last updated: 2026-06-20 (bug — layered convergence engine now delivers and addresses open human implementation feedback on a `implementation.human_review --revise--> implementation.build` round, fixing the no-op revise; enhancement: build pull-request title and body from the run's actual change — `ConvergenceRoundRecord.changedFilePaths`, `buildImplementationSummaryRoundInputs` in orchestrator, `mergeChangedFiles`/`summarizeChangedPaths` in implementation-summary, `deriveChangedPathSubject` in conventional-title, legacy placeholder filtering in pr-content, `provisionedBaseRef` in RunWorkspaceMetadata, `getChangedFiles` on RunWorkspaceGitPort, `pr-open-handler` diff recovery; enhancement: merged PR reconciliation trigger — `pr-reconciliation.ts` in api-contract, `reconcilePullRequests` in core service + routes, `reconcilePullRequests` in SDK, `PullRequestReconciliationTickerConfig` + `pr-reconciliation-ticker.ts` in control-plane)

> How agents navigate the codebase. Keep this current: whenever you add, move, or significantly
> change a module, update the relevant section in the same change.

## Source tree

- `apps/control-plane/` — bootable headless Fastify control-plane service. `src/config.ts` reads `CONTROL_PLANE_PORT`/`CONTROL_PLANE_DATABASE_PATH`/`CONTROL_PLANE_BEARER_TOKEN`/`CONTROL_PLANE_MASTER_SECRET`/`AUTOCATALYST_RUN_CONCURRENCY` (default `2`)/`AUTOCATALYST_REPOS_ROOT`/`AUTOCATALYST_WORKSPACES_ROOT`/`AUTOCATALYST_REAL_DISPATCH` (truthy: `1`/`true`/`yes`; falsy: `0`/`false`/`no`; absent = disabled)/`AUTOCATALYST_DEFAULT_PROVIDER_PROFILE_ID` (optional, only when real dispatch enabled) or CLI flags (`--port`, `--database-path`, `--bearer-token`, `--master-secret`, `--run-concurrency`, `--repos-root`, `--workspaces-root`, `--real-dispatch`, `--default-provider-profile-id`); exports `WorkspaceRootConfig`, `RealRunnerDispatchConfig`, `PullRequestReconciliationTickerConfig` (optional; enabled via `AUTOCATALYST_PR_RECONCILE_TICKER` truthy, interval via `AUTOCATALYST_PR_RECONCILE_INTERVAL_MS`, tenant override via `AUTOCATALYST_PR_RECONCILE_TENANT`; CLI flags: `--pr-reconcile-ticker`, `--pr-reconcile-interval-ms`, `--pr-reconcile-tenant`), and `ControlPlaneAppConfig` (workspace roots optional — both must be set together or neither; `realRunnerDispatch` optional — `defaultProviderProfileId` only valid when dispatch enabled); `src/server.ts` composes routes with persistence, unlocks `SqliteSecretStore` on startup, accepts `ControlPlaneServerOptions` (bearerToken, masterSecret, databasePath, runConcurrency, optional policy/health/extensionRegistry/providerAdapters/onProviderComposition, optional `unitOfWork: RunUnitOfWork` and `onControlPlaneReady(service)` for tests, optional `realRunnerDispatch: { enabled, defaultProviderProfileId }`, optional `workspaceRoots: WorkspaceRootOptions`), wires `InMemoryRetainedRunEventStore`, `RunDispatchQueue`, `DefaultOrchestrator`, and `DefaultControlPlaneService`; reads persisted configuration records once during startup, invokes `composeConfiguredProviders`, exposes the structured result through `onProviderComposition`, and logs sanitized production diagnostics through `logProviderCompositionDiagnostics`; when `realRunnerDispatch.enabled=true`, auto-registers the Claude adapter (`createClaudeAgentAdapter`) into the adapter map if not already present, builds a `AgentProviderAdapterRegistry` via `composeAgentProviderAdapterRegistry`; **routing-first dispatch:** builds `createModelRoutingResolver` from tenant-scoped configuration records and adapter registries, exposes `createRoutingProfileResolver(options)` which bridges `AgentRunnerFactoryInput`/`DirectCallFactoryInput` to the model routing resolver (validates role with `sessionRoleSchema`, throws `ModelRoutingConfigurationError('route_not_found')` for invalid role); production dispatch is routing-first, falls back to `defaultProviderProfileId` only when `routing_table_missing` AND an explicit profile is configured; convergence receives a fallback-aware `ModelRoutingResolver` wrapper (`createDefaultProviderProfileFallbackRoutingResolver`) so reviewed steps such as `implementation.build` also use the explicit default profile when no active routing table exists, while same-profile fallback is surfaced as non-distinct reviewed routing (`role_distinct_unsatisfied`); also wires `createExplicitProfileResolver` (looks up `defaultProviderProfileId` configuration record and builds a `ResolvedAgentRunnerProfile`/`ResolvedAgentCredentialReference`), creates a `AgentRunnerFactory` via `createAgentRunnerFactory`, wraps it in a `createDelegatingExecutionEntryPoint` (adds temp-dir scratch root for `workspace.shape='none'` runs), and chains through an async `resolveContext` wrapper + `createExecutionRunUnitOfWork`; **spec-authoring context wiring:** `resolveContext` now loads spec-authoring context (via `loadSpecAuthorPromptInput` from `spec-authoring-context-loader.ts` + `buildSpecAuthorContext` from core) for feature/enhancement runs at the `spec.author` step, passing function-valued async `prompt`/`taskInputs` callbacks to `createExecutionContextResolver`; `resolveContext` also calls `buildImplementationBuildContext` for `implementation.build` role sessions (both implementer and reviewer) and returns role-aware prompt and task inputs; **`resolveScratchResultValidationConfig`** helper selects the reviewer result contract (`registerReviewerResultContract`) for `implementation.build` reviewer sessions so the execution entry point validates reviewer scratch output against `reviewerResultSchema`; `stepResultContractRegistry` chains `registerPullRequestFinalizeResultContract(registerReviewerResultContract(registerSpecAuthorResultContract(...)))` so all three contracts are registered; current `pr.finalize` results flow through the core bridge (`validatePullRequestFinalizeResult`), not scratch-file validation in the entry point; **workspace context loading for two-root runs:** `resolveWorkspaceInputForRun` loads `Run.topicId → Topic → Conversation → Project` through `domainRepos`, validates tenant on each entity, derives `topicSlug` and `shortRunId`, and supplies the full `WorkspaceResolverInput` (project, roots, topicSlug, shortRunId) to a per-run `createExecutionContextResolver`; workspace-backed work kinds: `feature`, `enhancement`, `bug`, `chore`, `file_issue`; `question` maps to `none` shape and skips this loading; `resolveRole` seam defaults every step to `'implementer'` (per-step role catalog not yet wired — see `decisions/model-routing-resolve-role-default.md`); exported helpers: `createExplicitProfileResolver`, `createRoutingProfileResolver`, `createDefaultProviderProfileFallbackRoutingResolver`, `logProviderCompositionDiagnostics`, `createControlPlaneServer`, `startControlPlaneServer`, `WorkspaceRootOptions`; `src/pr-reconciliation-ticker.ts` — no-overlap configured-tenant background ticker; `createPullRequestReconciliationTicker(options)` starts a `setInterval`-based loop at the configured `intervalMs`; each tick calls `controlPlaneService.reconcilePullRequests` for the configured tenant; overlapping ticks are skipped via an in-flight guard; exports `PullRequestReconciliationTicker` interface and `createPullRequestReconciliationTicker` factory; B1 limitation: uses one explicitly configured tenant (no all-tenant open-PR discovery API exists yet). `src/main.ts` is the executable entrypoint; `src/integration.spec.ts` proves health, probe-resource persistence across restart, SSE, degraded database health, startup provider composition with fake adapters, and orchestrator ingress end-to-end over the network (`POST /v1/conversations`, `GET /v1/runs/:id`, `GET /v1/runs/:id/steps`, SSE `GET /v1/runs/:id/events` with a real `run_state_transition` frame after a tick); **feature dispatch workspace context regression** (`real feature dispatch workspace context`): drives a feature run through production dispatch path (real server, no injected unitOfWork), uses a fake OpenAI agent adapter that captures `workspace.shape`, `workspace.workspaceRoots.length`, and `workspaceIntent.provisioning.project.id` at `startSession` time, then asserts `two_roots` shape, 2 workspace roots, and correct project ID; requires a real local git repo and stored credential secret so `createAgentConnection` can satisfy `required: true`; **spec-authoring integration:** `src/integration.spec.ts` also proves the spec-authoring context flow end-to-end using `spec-authoring-harness.spec-helper.ts`; `src/control-plane-service.integration.spec.ts` proves the same end-to-end path at the service level with a real SQLite DB + `DrizzleConversationIngressRepository` + `DefaultOrchestrator` + `InMemoryRetainedRunEventStore` + `DefaultControlPlaneService`. **NEW — issue reference integration:** `src/issue-reference-create.integration.spec.ts` — deterministic fake-`gh` integration test (7 tests): structured `issue_reference` submission, free-form text recognition, token not in response, failure → `intake_routing_error`, no run created on failure, ambiguous references → `intake_routing_error`, token not in thrown errors. Uses a shell-script fake `gh` executable written to a temp directory and `SqliteSecretStore` seeded with a sentinel token. `src/server.ts` now accepts optional `githubIssueTracker?: { executablePath?, timeoutMs? }` in `ControlPlaneServerOptions` and composes `GitHubIssueTracker` + `StaticIssueTrackerRegistry` + `DefaultIssueReferenceIntakeResolver` + passes `domainRepos.projects` to `DefaultControlPlaneService`. **NEW — spec-authoring context loader:** `src/spec-authoring-context-loader.ts` — loads tenant-scoped run → topic → conversation → project → messages plus optional linked issue metadata from `DomainRepositories`; returns `SpecAuthorPromptInput` for use with core builders. Tests: `src/spec-authoring-context-loader.spec.ts`. **NEW — spec-authoring harness helper:** `src/spec-authoring-harness.spec-helper.ts` — contract-aware fake adapter for integration testing; validates prompt contains `mm:planning`, ownership rules, and `step-result.json` contract; writes conformant or malformed scratch result on demand. Tests: `src/control-plane-service.integration.spec.ts`. **NEW — live provider spec:** `src/spec-authoring-live-provider.spec.ts` — optional live authentication proof; skipped unless `AUTOCATALYST_LIVE_CLAUDE_SPEC_AUTHOR=1` and `ANTHROPIC_API_KEY` are set. **NEW — convergence dispatch implementations:** `src/reviewed-execution-dispatcher.ts` — concrete `ReviewedRoleDispatcher` wrapping `ExecutionRunUnitOfWork`; forces `toolPolicyMode: 'read_only'` for every reviewer session before calling the underlying unit-of-work; parses `reviewerResult` and `dispositions` from checkpoint data after each round. `src/run-workspace-git-port.ts` — concrete `RunWorkspaceGitPort` implementation: uses git CLI subprocess to commit implementer output files; validates all paths stay within the declared workspace roots using `realpath` containment checks before any git operations. **Altitude-gate additions:** `captureCheckpointRef(input)` writes a git ref under `refs/autocatalyst/runs/<runId>/implementation.build/<altitude>`; accepts both `refs/...` symbolic refs and raw 4-64 char hex commit SHAs; rejects `..`, spaces, and other unsafe inputs. `readFileAtRef(input)` and `listFilesAtRef(input)` read file content and list paths at a given ref for drift detection. Tests: `src/run-workspace-git-port.spec.ts` (17 tests including checkpoint ref tests). `RunWorkspaceMetadata.provisionedBaseRef` is persisted from `@autocatalyst/execution` two-root provisioning through `apps/control-plane/src/server.ts` and is the preferred base for `pr.open` diff recovery after restarts. `src/pr-open-handler.ts` calls `RunWorkspaceGitPort.getChangedFiles` with that explicit base ref, then merges final branch diff paths with persisted cumulative summary paths before PR creation. PR code must not shell out for `git diff`; changed-file recovery stays behind the host-controlled workspace-git port, implemented in `apps/control-plane/src/run-workspace-git-port.ts`. `src/server.ts` — **altitude-gate addition:** uses `createLayeredConvergenceEngine` instead of `createConvergenceEngine` for the `implementation.build` convergence engine. **Convergence gotcha — provider read-only enforcement:** provider adapters that cannot enforce reviewer `read_only` tool policy must fail reviewer dispatch safely or grant no file/git access. The `ReviewedRoleDispatcher` seam forces `toolPolicyMode: 'read_only'` at the boundary; adapters that cannot honor this must surface `tool_policy_unsupported` rather than silently proceeding with unrestricted tools. **Smoke tests:** `src/implementation-build-convergence.smoke.spec.ts` — 5 production-path smoke tests: happy path (single-altitude build convergence), stall detection, full ladder (all four altitudes converge, 3 checkpoint refs captured), build drift (public_api export removed triggers build_drift blocker), and contract violation. Live-provider altitude smoke was not required for CI and was skipped unless AUTOCATALYST_LIVE_IMPLEMENTATION_CONVERGENCE=1 and provider credentials were present. Providers that cannot enforce reviewer read-only access remain unsupported for reviewed reviewer sessions and must fail safely or grant no file/git access.
- `packages/api-contract/` — shared API contract package. `src/health.ts`, `src/probe-resource.ts`, `src/errors.ts`, and `src/sse.ts` own Zod schemas, inferred types, route constants, and status constants. `src/principal.ts` owns `principalKindSchema`, `principalSchema`, `principalDiagnosticResponseSchema`, and `principalDiagnosticPath = '/v1/principal'`. `src/secret.ts` owns `secretHandleSchema`, `secretCollectionPath = '/v1/secrets'`, `createSecretRequestSchema`, and `createSecretResponseSchema`. `src/configuration-record.ts` owns full CRUD Zod schemas: `createConfigurationRecordRequestSchema`, `configurationRecordResponseSchema`, `configurationRecordListResponseSchema`, `updateConfigurationRecordRequestSchema`, `deleteConfigurationRecordSuccessStatusCode`, and `configurationRecordCollectionPath = '/v1/configuration-records'`; `CreateConfigurationRecordRequest`, `UpdateConfigurationRecordRequest`, and `ConfigurationRecord` are now kind-tagged discriminated unions with `tenant` field; routing-specific additions: `ModelRoutingTableSettings`, `UpdateModelRoutingTableSettings` (routing table config with entries and roleDistinctRequirements), `AgentModelRouteKey`, `DirectModelRouteKey`, `ModelRouteKey` (discriminated union on route key type), `ModelRoutingEntry`, `RoleDistinctRequirement` (routing entry and distinct-role constraint), `ModelRoutingErrorCode` (typed error code enum: `route_not_found`, `routing_table_missing`, `role_distinct_unsatisfied`, `ambiguous_route`, `invalid_route_key`). `src/errors.ts` also exports error code constants: `unauthorizedErrorCode`, `validationErrorCode`, `notFoundErrorCode`, `secretStoreLockedErrorCode`. `src/openapi.ts` generates OpenAPI from those schemas and constants. Domain entity schemas (all Zod, all with inferred TS types): `src/domain-value-objects.ts` — shared value objects: `ModelIdentity`, `TokenBreakdown`, `Cost`, `TrackedIssue` (**now enriched:** `body: z.string()` + `labels: z.array(z.string().min(1))`; `trackedIssueSchema` uses `z.preprocess` to default missing `body`/`labels` for legacy persisted records), `ChannelReference`, `FeedbackAnchor`, `FeedbackThread`, `TestingGuideResult`, `InferenceSettings`, `SessionRole`, `FrontedResource`, `CredentialReference`, `JsonValue`, `NonModelPrincipal`; also exports `requireTenantMatchesOwner` helper. `src/project.ts` — `Project` + `CreateProjectInput`. `src/conversation.ts` — `Conversation` + `CreateConversationInput` (nullable `activeTopicId`). `src/topic.ts` — `Topic` + `CreateTopicInput` (`kind: main|side`, no `isActive` marker). `src/message.ts` — `Message` + `CreateMessageInput` (`direction: inbound|outbound`). `src/run.ts` — `Run` + `CreateRunInput` (`currentStep`: extensible string, `terminal`: bool; no `status`/`RunStatus`). `src/artifact.ts` — `Artifact` + `CreateArtifactInput` (`kind`, `canonicalRecord`, `cachedStatus` enums). `src/feedback.ts` — `Feedback` + `CreateFeedbackInput` (`target`, `status` enums, optional `anchor`, `thread`). `src/publication.ts` — `Publication` + `CreatePublicationInput` (`frontedResource`). `src/pull-request.ts` — `PullRequest` + `CreatePullRequestInput` (`state: open|merged|closed`). `src/run-step.ts` — `RunStep` + `CreateRunStepInput` (no owner/tenant — parent-hung). `src/session.ts` — `Session` + `CreateSessionInput` (`tokens` must equal `cost.tokens`; no owner/tenant). `src/test-result.ts` — `TestResult` + `CreateTestResultInput` (no owner/tenant). Runner event vocabulary: `src/runner-events.ts` owns `runnerEventTypeSchema`, `runnerEventImportanceSchema`, `runnerTerminalDirectiveSchema`, `runnerEventSchema` (discriminated union on `type`), and inferred types `RunnerEvent`, `RunnerAssistantTurnEvent`, `RunnerToolActivityEvent`, `RunnerProgressEvent`, `RunnerNotificationEvent`, `RunnerStepCheckpointEvent`, `RunnerTerminalResultEvent`. Post-validation handoff schema: `src/step-results.ts` owns `RunnerTerminalStepResult` and `runnerTerminalStepResultSchema` — these are the post-validation execution-to-core handoff schemas (`advance` with optional `result: Record<string,unknown>`, `needs_input`, `fail`); separate from the raw runner terminal result in `runner-events.ts`; used by `execution-boundary-events.ts` terminal event and consumed by core's `execution-run-unit-of-work.ts`. Also owns `prFinalizeFindingSchema` (`severity: enum(['blocker','warning','info'])`, `summary`, optional `target`; `.strict()`) and `prFinalizeResultSchema` (`directive: enum(['advance','revise'])`, optional `reconciledSummary`/`titleSubject`/`validationSummary`, `findings` defaulting to `[]`; `.strict()`); these are the shared PR-finalize schemas used by both execution and core to avoid package cycles; inferred types `PullRequestFinalizeFinding` and `PullRequestFinalizeResult` are re-exported from `src/index.ts`. Declarative execution context: `src/execution-context.ts` owns `executionContextSchema`, `workspaceProvisioningIntentSchema`, `secretBindingSchema`, and inferred types `ExecutionContext`, `WorkspaceProvisioningIntent`, `SecretBinding`; also exports `skillRefSchema` (reference to a named skill by catalog key), `resolvedSkillSchema` (a skill with its resolved asset path), and `skillIntentSchema` (the union of skill intents used in execution context). Spec authoring schemas: `src/spec-authoring.ts` — shared `SpecAuthorResult`/frontmatter/kind Zod schemas used by execution result contracts and core spec authoring. **NEW — spec review API surface:** `src/run-spec.ts` — `runSpecPath = '/v1/runs/:id/spec'`, `getRunSpecSuccessStatusCode`, `runSpecResponseSchema` (`{ artifact, markdown, frontmatter }`), `RunSpecResponse`; spec endpoint returns the rendered spec artifact with parsed frontmatter. `src/feedback.ts` also exports route-level additions: `runFeedbackPath = '/v1/runs/:id/feedback'`, `createRunFeedbackSuccessStatusCode` (201), `listRunFeedbackSuccessStatusCode` (200), `createRunFeedbackRequestSchema` (`{ target: 'artifact', title, body, anchor? }`), `runFeedbackListResponseSchema` (`{ feedback: Feedback[] }`), `CreateRunFeedbackRequest`, `RunFeedbackListResponse`. **NEW — run reply API surface:** `src/run-replies.ts` — `runRepliesPath = '/v1/runs/:id/replies'`, `postRunReplySuccessStatusCode`, `RunReplyRequest`/`RunReplyResponse`/`RunReplyClassification` Zod schemas and inferred types; discriminated union covering `approve`, `feedback`, and `guidance` reply kinds; `classifyRunReply(request)` returns the classification used by the route handler. `src/run.ts` exports `waitingOnSchema`, `RunWaitingOn`, and a `waitingOn` optional field on `runSchema` — derived from the run-step catalog at read time, never persisted; tells callers what external action is needed before the run can advance. `src/run.ts` includes optional public `failureReason` on `Run`; `src/run-events.ts` allows `transition.reason` only on `fail` `run_state_transition` events. **NEW — convergence schemas:** `src/convergence.ts` — shared Zod schemas and TypeScript types for the producing-step convergence loop. Key exports: `reviewerFindingSeveritySchema` (`blocker|warning|info`), `reviewerResultSchema` (reviewer session output with findings array), `findingDispositionSchema` (implementer's per-finding response: `addressed|wont_fix|acknowledged`), `convergenceCheckpointSchema` (stored in `RunStep.checkpointResult` for mid-round persistence), `convergenceRoundRecordSchema` (immutable record of one completed round). **Altitude-gate additions:** `implementationAltitudeSchema` (`layout|public_api|private_api|build`), `implementationConvergenceDepthSchema` (`build_only|layout|public_api|private_api`), `convergenceFindingSourceSchema` (`reviewer|deterministic`), `convergenceFindingCategorySchema` (`layout|public_api|private_api|build`); altitude-aware `convergenceRoundFindingSchema` (adds `source` and `category` fields), `convergenceRoundRecordSchema` (adds `altitude` field; legacy rounds default altitude to `'build'` for migration tolerance), `convergenceCheckpointSchema` (adds `depth`, `currentAltitude`, and `acceptedCheckpoints` fields), `altitudeCheckpointRefSchema` (captures a git ref and the altitude it represents). `altitude-contract-validator` is the AST-based early-altitude validator; `layered-finding-filter` demotes out-of-scope findings at early altitudes. Tests: `__tests__/convergence.spec.ts` (`pnpm nx test api-contract -- convergence.spec`). Tests: `__tests__/domain-value-objects.spec.ts`, `__tests__/domain-entities.spec.ts`. **NEW — PR reconciliation API surface:** `src/pr-reconciliation.ts` — exports `pullRequestReconciliationPath = '/v1/pull-requests/reconcile'`, `pullRequestReconciliationResponseSchema` (`{ checked, merged, closed, failed, timedOut }`), `reconcilePullRequestsSuccessStatusCode` (200), and inferred type `ReconcilePullRequestsResponse`; used by the explicit reconcile endpoint and SDK helper for `POST /v1/pull-requests/reconcile`. Public entry point: `packages/api-contract/src/index.ts`.
- `packages/core/` — control-plane core package. Depends on `@autocatalyst/execution` (vite alias for tests, workspace dep in package.json). Execution-adapter modules: `src/runner-event-stream.ts` owns `consumeRunnerEventStream` which validates schema (via `runnerEventSchema.safeParse`), enforces single-terminal protocol, checks runId, wraps `onEvent` hook throws as `RunnerProtocolError('runner_failed')`, and throws `RunnerProtocolError` for all protocol violations; `src/execution-run-unit-of-work.ts` owns `createExecutionRunUnitOfWork` which wires `resolveContext → execute → validateExecutionBoundaryEventStream` (from `@autocatalyst/execution`) and maps terminal directives (advance/needs_input/fail) to `RunWorkResult`; `advance` terminal events carry an optional `result: Record<string,unknown>` field that is forwarded to `RunWorkResult.advance.result`; `RunnerProtocolError` from protocol violations are re-thrown, runner errors before terminal map to a static, sanitized `{ directive: 'fail', reason }` using the error's type/code only (never raw `error.message`, which may contain sensitive data): `ExecutionMaterializationError` → `'Execution failed: <code>'`; all others → static string; `src/execution-context-resolver.ts` owns `createExecutionContextResolver(options)` factory which builds a declarative `ExecutionContext` from `RunWorkInput` — maps work kinds to workspace shapes (feature/enhancement/bug/chore → two_roots, question → none, file_issue → scratch_only), validates secret envName format, and throws `ExecutionContextResolutionError` with codes: `unsupported_work_kind`, `unsupported_workspace_shape`, `missing_project`, `missing_workspace_settings`, `invalid_secret_declaration`, `resolver_unavailable`. `src/secret.ts` (augmented) additionally exports `SecretResolutionErrorCode`, `SecretResolutionError` class, `SecretResolver` interface, and `SecretResolutionErrorDetails`. New ingress modules: `src/orchestrator.ts` owns `Orchestrator`/`DefaultOrchestrator` with `createRun`, `createConversationWithFirstRun`, `applyDirective`, `dispatch`, `tick`, `replyToRun` (applies origin-aware directive: approve resolves open gate blockers and advances, guidance/feedback sets needs_input with text payload, serialized per run via `enqueueForRun`), the `RunUnitOfWork` seam (`{ run(input: RunWorkInput): Promise<RunWorkResult> }`), and `OrchestratorError`/`OrchestratorErrorCode` (`active_run_conflict`/`missing_run`/`terminal_run`/`invalid_transition`/`unknown_work_kind`/`forbidden`/`persistence_failed`); `DefaultOrchestrator.dispatch` routes `intake` as a deterministic system step and refuses to send unimplemented system side-effect steps such as `pr.open` and `issues.file` to the runner; **Auto-dispatch (issue 45):** `DefaultOrchestrator` also exports `AutoDispatchOptions` (`{ enabled?: boolean }`) and accepts `autoDispatch?: AutoDispatchOptions` in `DefaultOrchestratorOptions`; auto-dispatch is **enabled by default** — production paths pass no `autoDispatch` option; tests that require exact synchronous control pass `autoDispatch: { enabled: false }`; private `#shouldAutoDispatch(run)` reads `getRunStepDefinition(run.currentStep).waitingOn` and returns true only for `'system'` or `'ai'` — `'human'`, `'none'`, and unknown steps return false; private `#scheduleAutoDispatch(run)` is called after every durable state change and its state-transition event publication (in `createRun`, `createConversationWithFirstRun`, and `applyDirective`); the scheduled dispatch is **fire-and-return** — callers of create/applyDirective do not await runner execution; **In-flight duplicate guard:** `#autoDispatchInFlightRunIds: Set<string>` blocks a second automatic schedule for a run already pending or active through auto-dispatch within this process; the guard is released in `finally` and does not block explicit `dispatch` or `tick` calls; **Detached failure handling:** `#handleAutoDispatchFailure` catches every rejection from the detached promise, logs only sanitized details (error code + run id, never raw provider messages), and — if the run is still on an eligible non-terminal step — calls `applyDirective({ directive: 'fail' })` to record failure through the normal path; if the run has already advanced to human/terminal state, the handler silently no-ops to avoid overwriting newer state; **`tick` is an explicit fallback seam**, not the normal create-and-progress path — normal production runs advance through auto-dispatch; `tick` remains useful for recovery and for sources that cannot push; `src/control-plane-service.ts` owns the `ControlPlaneService` facade (`DefaultControlPlaneService`) consumed by routes — `createConversationWithFirstRun`, `getRun`, `listRunSteps`, `subscribeRunEvents`, `tick`; **NEW** `getRunSpec` (reads spec artifact file from the run's workspace via `workspaceFilesystem`/`runWorkspaceMetadata` seam, parses frontmatter, renders markdown), `createRunFeedback` (creates artifact feedback for a run, owner/tenant from principal, status always `'open'`), `listRunFeedback` (lists all feedback for a run scoped to caller's tenant); **NEW** `replyToRun(id, request, principal)` — classifies the incoming `RunReplyRequest` and dispatches to orchestrator with origin-aware directive application (approve → advance with approval metadata, feedback → needs_input with guidance payload, guidance → needs_input with guidance text); also exports `mapRunToApiRun` helper that decorates a raw `Run` with the `waitingOn` field derived from `run-step-catalog`; `DefaultControlPlaneService` constructor now accepts `workspaceFilesystem` and `runWorkspaceMetadata` deps for spec reading — plus `ControlPlaneServiceError`; `src/run-events.ts` owns `RunEventBus`/`RunEventPublisher`/`RunEventSubscriber`, `RunEventSubscription`, `createRunStateTransitionEvent`, and the live-only `InMemoryRunEventBus` (per-(runId,tenant) subscribers, no durable replay; subscribers must register before the transition they want to observe); `src/run-dispatch-queue.ts` owns the bounded `RunDispatchQueue` (positive-integer `maxConcurrent`, `activeCount`/`queuedCount`, FIFO, slot released even when work rejects); now also exposes `enqueueForRun(runId, fn)` for per-run serialization — ensures concurrent operations on the same run are queued rather than racing. `src/health.ts` owns dependency health behavior; `src/probe-resource.ts` owns proof-resource use cases and repository interfaces; `src/routes.ts` registers Fastify routes using contract schemas — now includes `POST /v1/runs/:id/replies` which routes approve/feedback/guidance replies to `controlPlaneService.replyToRun`; `src/principal.ts` owns `principalSymbol` (Symbol-keyed request context), `attachPrincipalToRequest`, `requirePrincipalFromRequest`, and `hardcodedDevelopmentPrincipal`; `src/auth.ts` owns `registerBearerAuthHook` (Fastify preHandler hook using `timingSafeEqual`) and `BearerAuthOptions`; `src/policy.ts` owns `PolicyDecisionPoint` interface, `PolicyDecisionInput`, `PolicyResourceDescriptor` (discriminated union), `permissivePolicyDecisionPoint`, and `authorizeRequest`; `src/configuration-record.ts` owns `ConfigurationRecordRepository` interface and five thin use-case functions (`createConfigurationRecord`, `listConfigurationRecords`, `getConfigurationRecord`, `updateConfigurationRecord`, `deleteConfigurationRecord`); `src/secret.ts` owns `SecretStore` interface, `SecretStoreLockedError`, and `createSecret` use-case; `src/extension-registry.ts` owns metadata-only provider registry types, `InMemoryExtensionRegistryCatalog`, `defaultExtensionRegistryCatalog`, and advisory registry-warning generation; `src/provider-composition.ts` owns provider adapter-map types, `buildProviderAdapterKey`, `emptyProviderAdapterMap`, and `composeConfiguredProviders` startup composition diagnostics; provider adapter registries de-duplicate multiple configuration profiles that share the same adapter instance while still rejecting conflicting duplicate adapter instances; `src/domain-repositories.ts` — narrow repository interfaces for all 12 domain entities (`ProjectRepository`, `ConversationRepository`, `TopicRepository`, `MessageRepository`, `RunRepository`, `ArtifactRepository`, `FeedbackRepository`, `PublicationRepository`, `PullRequestRepository`, `RunStepRepository`, `SessionRepository`, `TestResultRepository`) and the `DomainRepositories` collection type; no Drizzle imports — and lifecycle-recording methods `recordRunLifecycleStart` and `recordRunStepTransition` on `RunRepository` (atomic start/transition writes); lifecycle input/output types `RecordRunLifecycleStartInput`, `RecordRunLifecycleStartResult`, `RecordRunStepTransitionInput`, `RecordRunStepTransitionResult`, `LifecycleRunStepInput`; `src/run-step-catalog.ts` — run lifecycle step primitives: 18 step definitions, `waitingOn`/`phase`/`roles` catalog, derived sets `terminalSteps`/`modelActiveSteps`/`messageAcceptingSteps`, and helpers `getRunStepDefinition`, `isKnownRunStepId`, `deriveRunTerminal`; `src/run-workflows.ts` — workflow-as-data definitions for `feature`, `enhancement`, `bug`, `chore`, `file_issue`, `question` with exact step paths, transition tables (advance/revise/needs_input/pause-resume edges), and helpers `getRunWorkflowById`, `getRunWorkflowForWorkKind`, `isKnownRunWorkflowId`; `src/run-transition.ts` — pure `nextWorkflowStep(workflow, step, directive) → TransitionResult` transition rule with all typed `TransitionErrorCode` values; `src/run-lifecycle.ts` — `startRunLifecycle` and `applyRunDirective` use cases over `RunRepository` with `RunLifecycleError` and all `RunLifecycleErrorCode` values. **Failure reason flow:** `RunWorkResult.fail.reason` → `DefaultOrchestrator.applyDirective` → `normalizeFailureReasonForPublicSurface` → `applyRunDirective(reason)` → `RunRepository.recordRunStepTransition(failureReason)` → `createRunStateTransitionEvent(reason)`. Unknown strings become `runner_failed_before_terminal_result`; synthesized auto-dispatch/spec-authoring failures use stable safe codes. Reviewer dispatch failures are first-class in `convergence-engine.ts` and `layered-convergence-engine.ts`: reviewer `fail` and `needs_input` results are returned before reviewer result parsing, while `reviewer_result_missing` only means a reviewer advanced without a parseable reviewer result. `safe-failure-reason.ts` maps classified provider failures and provider-shaped transient status errors to public-safe codes without reading raw error messages. **NEW — spec authoring and feedback gate:** `src/spec-frontmatter.ts` — committed spec frontmatter parse/render/validation and final Markdown rendering; `src/spec-authoring-service.ts` — ordered validated result → spec file write → commit → file-canonical Artifact path; `src/feedback-lifecycle.ts` — artifact feedback create/address/resolve/wont_fix/reopen/co-resolution use cases; `src/spec-review-gate.ts` — explicit `spec.human_review` advance guard for open/addressed artifact feedback; `src/spec-approval-finalizer.ts` — approval-time frontmatter status commit and Artifact cached-status update. **NEW — model routing:** `src/model-routing-resolver.ts` exports `createModelRoutingResolver(options)` factory (builds `ModelRoutingResolver`) and `ModelRoutingConfigurationError` (typed error class with `code: ModelRoutingErrorCode` and `safeDetails`). Resolver methods: `resolveAgentRoute(step, role)` — exact `(step, role)` lookup, falls back to step-level default; `resolveDirectRoute(step)` — direct-only lookup; `resolveDistinctAgentRoutes(input)` — resolves per-role distinct route groups with collision detection; deduplicates `input.roles` via `new Set(...)` before collision check (see `decisions/model-routing-role-deduplication.md`); table-defined `distinctBy` overrides caller-provided `distinctBy` (see `decisions/model-routing-distinct-by-precedence.md`). Reads from `ModelRoutingConfigurationReader` seam (two methods: `listConfigurationRecords`, `findConfigurationRecordById`). **NEW — spec authoring context:** `src/spec-authoring-context.ts` — provider-neutral `spec.author` prompt and task-input builders; enforces feature/enhancement-only authoring; emits safe typed builder errors (`SpecAuthorContextError` with codes); keeps runtime ownership + `mm:planning` instructions aligned with `autocatalyst.spec_author.v1` result contract; `specced_by` is NOT required from the model — the system stamps it, and the prompt explicitly tells the model not to invent it; `trustedSpeccedBy: 'autocatalyst'` is included in output contract. Tests: `src/spec-authoring-context.spec.ts`. **NEW — implementation build context:** `src/implementation-build-context.ts` — `buildImplementationBuildContext(input)` returns role-aware `{ prompt, taskInputs }` for `implementation.build` implementer and reviewer sessions; implementer prompt points to the approved spec, altitude, dispositions, and required output shape; reviewer prompt includes exact `reviewerResultSchema` JSON examples for `{ status: 'satisfied' }` and `{ status: 'findings' }`, read-only enforcement instructions, and explicit prohibition against writing a feature_spec; reviewer task inputs include `outputContract.schemaId: 'autocatalyst.reviewer_result.v1'`. Exported from `packages/core/src/index.ts`. **Updated — execution context resolver:** `src/execution-context-resolver.ts` now supports async `prompt` and `taskInputs` callbacks returning `string | undefined | Promise<string | undefined>` and `Record<string, unknown> | undefined | Promise<Record<string, unknown> | undefined>` respectively (previously synchronous only). Tests: `src/execution-context-resolver.spec.ts`. **NEW — reviewer-result validation helper:** `src/reviewer-result-validation.ts` — Shared reviewer-result tolerance helper for convergence engines; wraps `validateStepResult` with `reviewerResultSchema` and maps unrecoverable validation to `reviewer_result_invalid`. Used by both `convergence-engine.ts` and `layered-convergence-engine.ts`; `reviewerResultCorrectionRequester` option is accepted but not wired in any production site (provider-backed correction is not enabled in this slice).
**NEW — convergence loop:** `src/convergence-engine.ts` — generic bounded implementer/reviewer convergence loop. `createConvergenceEngine(options)` returns an engine that runs at most `maxRounds` implementer/reviewer pairs for a single producing step. Internal helpers: `findingSignature` (stable key for dedup), `isBlockingFinding` (severity-based gate), `computeCurrentBlockingSet` (active blockers after dispositions applied), `detectOscillation` (same blocker set seen across consecutive rounds), `resolveReviewedRoutes` (picks per-role dispatch targets). Called by `DefaultOrchestrator` for AI steps that carry both implementer and reviewer roles. Engine returns a `ConvergenceOutcome` (`converged|max_rounds_reached|oscillation_detected|stalled`) with an array of `ConvergenceRoundRecord`s. Tests: `src/convergence-engine.spec.ts` (32 tests — `pnpm nx test core -- convergence-engine.spec`). `src/reviewed-role-dispatcher.ts` — provider-neutral seam for role-aware session dispatch. Interface `ReviewedRoleDispatcher.runRole(input: RunRoleWorkInput)`. Reviewer sessions are always forced to `toolPolicyMode: 'read_only'` at this boundary regardless of caller input. `src/run-workspace-git.ts` — host-controlled workspace git port. Interface `RunWorkspaceGitPort.commitFiles(...)` for committing implementer session output from the host side. `defaultReviewerWorkspacePolicy` exports a forbidden git-action list used by reviewer sessions. `src/convergence-feedback.ts` — maps reviewer findings to `Feedback` records. `createReviewerFeedback(input)` factory. Maps `spec.author` step to `artifact` feedback target; `implementation.build` step to `implementation` target; now includes target-aware gate feedback creation, blocker listing, approver co-resolution, and producer disposition helpers. **NEW** `src/human-review-gate.ts` — maps a human review step to its feedback target kind and provides the generic `isApprovalBlocked(runId, step, repos)` guard used by the orchestrator before allowing approval-type directives to advance a run. **NEW** `src/convergence-checkpoint.ts` — durable `implementation.awaiting_input` convergence escalation pause helpers: `getConvergenceEscalationPause(input)` reads an existing pause record from the checkpoint, `recordConvergenceEscalationGuidance(input)` persists human guidance into the checkpoint so the convergence engine can resume with the caller-supplied text. `src/convergence-policy.ts` — `getStepConvergencePolicy(workflow, step)` returns the max-round config for a step. Default is 3 rounds. Configurable via `RunWorkflowDefinition.convergence` field. **Altitude-gate additions:** `ResolvedStepConvergencePolicy` now includes a `depth` field (`ImplementationConvergenceDepth`); `StepConvergencePolicyError` is a typed error for invalid policy configuration; `getImplementationAltitudeLadder(depth)` returns the ordered altitude sequence for a given depth. `src/run-workspace-git.ts` — **checkpoint ref additions:** `CheckpointAltitude` type alias; `CaptureCheckpointRefInput`/`CaptureCheckpointRefResult` types for the new `RunWorkspaceGitPort.captureCheckpointRef(input)` method; `ReadFileAtRefInput`/`ListFilesAtRefInput` and matching methods `readFileAtRef`/`listFilesAtRef` for drift detection; ref namespace is `refs/autocatalyst/runs/<runId>/implementation.build/<altitude>`. Agents must not create checkpoint refs directly; the host git port owns refs under `refs/autocatalyst/...`. Provider adapters that cannot enforce reviewer read-only access remain unsupported and must fail safely or grant no file/git access. `src/altitude-contract-validator.ts` — **NEW:** TypeScript AST-based validator for early altitude contracts (`layout`/`public_api`/`private_api`). Rejects test files, JSX, function bodies, class bodies, initializers, runtime enums, and side-effect imports. Uses `ts.createSourceFile` — NOT regex. Entry point: `validateAltitudeContract(input)` returns `AltitudeContractViolation[]`. Tests: `src/altitude-contract-validator.spec.ts` (`pnpm nx test core -- altitude-contract-validator.spec`). `src/layered-finding-filter.ts` — **NEW:** Altitude allowlist filter. `filterAltitudeFindings(findings, altitude)` demotes out-of-scope reviewer findings at early altitudes; deterministic findings keep their original blocking value unchanged. Tests: `src/layered-finding-filter.spec.ts` (`pnpm nx test core -- layered-finding-filter.spec`). `src/build-contract-preservation.ts` — **NEW:** Build-time drift detector. `extractContractSnapshot(input)` extracts TypeScript public/private signatures from accepted checkpoint refs. `validateBuildContractPreservation(input)` compares the accepted snapshot against the build commit and returns `BuildContractViolation[]` for removed files, removed exports, and signature changes. Tests: `src/build-contract-preservation.spec.ts` (`pnpm nx test core -- build-contract-preservation.spec`). `src/layered-convergence-engine.ts` — **NEW:** ~720 LOC ladder coordinator. `createLayeredConvergenceEngine(options)` resolves policy/ladder via `getImplementationAltitudeLadder`, resolves routes once for all altitudes, runs per-altitude bounded round loops, invokes `validateAltitudeContract` after each early implementer commit, invokes `validateBuildContractPreservation` before build acceptance, filters findings with `filterAltitudeFindings`, persists via `createConvergenceFeedback`, captures checkpoint refs via `git.captureCheckpointRef` at non-final altitudes. **Human revise feedback:** `runEngine` loads open human-authored implementation `Feedback` (`target: implementation`, `status: open`, first thread author `kind: 'human'`) once and threads it into the `build` altitude only — delivered to the implementer as a required disposition, kept blocking until the implementer disposes of it (so a zero-change revise round cannot converge), and marked `addressed` (keyed to `fixed`-disposed feedback ids; the approval gate's `resolveApproverAddressedFeedback` resolves it on human approve) on convergence. This is the production engine for `implementation.build`; `createConvergenceEngine` (the build-only wrapper) has its own equivalent seed/address logic. See `decisions/layered-engine-human-revise-feedback.md`. Returns `LayeredConvergenceOutput` with `workResult`, `checkpointResult` (includes `depth`, `currentAltitude`, `acceptedCheckpoints`), and `rounds`. Tests: `src/layered-convergence-engine.spec.ts` (15 tests — covers `build_only`, `layout`, full depth, round reset, escalation, deterministic blocker lifecycle, open human implementation feedback delivery/addressing and the no-op revise invariant; `pnpm nx test core -- layered-convergence-engine.spec`). Old no-depth workflows remain build-only: `build_only` depth test asserts `checkpoint.depth === 'build_only'`, all rounds have `altitude === 'build'`, and `acceptedCheckpoints` is empty. `src/convergence-engine.ts` — **Altitude update:** sets `altitude: 'build'` on all round records; `createConvergenceEngine` remains as build-only compatibility wrapper; the full ladder path uses `createLayeredConvergenceEngine` instead. **Updated — orchestrator convergence wiring:** `src/orchestrator.ts` `DefaultOrchestratorOptions` now accepts optional `convergenceEngine?: ConvergenceEngine` and `runSteps?: RunStepRepository`. Private `#isReviewedProducingStep(step)` helper returns true for steps whose catalog entry includes both `implementer` and `reviewer` roles — these steps are routed through the convergence engine; all other AI steps use the existing one-shot dispatch path. **NEW — code-host port and registry (T-001):** `src/code-host.ts` — provider-neutral `CodeHostPort` interface (`create`, `read`, `findByBranch`, `update`, `merge`); input/output types (`CodeHostTarget`, `CodeHostCredential`, `PullRequestContent`, `CodeHostPullRequestFacts`, `CreateCodeHostPullRequestInput`, `ReadCodeHostPullRequestInput`, `FindCodeHostPullRequestByBranchInput`, `UpdateCodeHostPullRequestInput`, `MergeCodeHostPullRequestInput`, `MergeStrategy`); `CodeHostErrorCode` union (9 codes); `CodeHostError` class (strips unsafe keys from `safeDetails` via `sanitizeCodeHostDetails`; safe keys: `provider`, `repository`, `owner`, `name`, `branch`, `headBranch`, `baseBranch`, `number`, `state`, `url`); `isCodeHostError` guard; `repositorySlug` helper; `defaultMergeStrategy()` returns `{ method: 'squash', deleteBranch: true }`. `src/code-host-registry.ts` — `CodeHostProviderRegistration` (provider name + factory), `CodeHostRegistry` interface (`get(provider): CodeHostPort`), `createCodeHostRegistry(registrations)` factory: throws `duplicate_provider` on duplicate registration, throws `unsupported_provider` for unknown lookups. Both modules exported from `packages/core/src/index.ts`. Tests: `src/code-host.spec.ts` (8 tests), `src/code-host-registry.spec.ts` (6 tests). **NEW — PR lifecycle feature (T-015):** `src/implementation-summary.ts` — `CumulativeImplementationSummary` interface + type guard + `requireCumulativeImplementationSummary`; `buildCumulativeImplementationSummary(rounds)` folds successive summary text by appending (not replacing), deduplicates file and validation lists across rounds. `src/conventional-title.ts` — `deriveConventionalTitle(input)` builds `feat:`, `fix:`, or `chore:` prefixed title from `workKind` + `titleSubject` or `summary`; `normalizeConventionalSubject(subject)` lowercases, strips trailing punctuation, truncates at 72 chars. `src/pr-content.ts` — `buildPullRequestContent(input)` assembles `PullRequestContent` (title + body) from cumulative implementation summary, reconciled summary, and issue URL. `src/pr-finalize.ts` — `buildPullRequestFinalizePrompt(input)` emits a read-only reviewer prompt with explicit no-write instructions; `parsePullRequestFinalizeResult(value)` is **deprecated** strict Zod parse (no tolerance pipeline) — use `validatePullRequestFinalizeResult` from `pr-finalize-result-validation.ts` instead; `buildPullRequestFinalizeCheckpoint(result)` persists the reviewer decision; `feedbackInputsFromPullRequestFinalizeFindings(...)` maps blocker/warning findings to `Feedback` creation inputs; `prFinalizeResultSchema`/`PullRequestFinalizeFinding`/`PullRequestFinalizeResult` are now re-exported from `@autocatalyst/api-contract`. **NEW — pr-finalize result validation bridge:** `src/pr-finalize-result-validation.ts` — `validatePullRequestFinalizeResult(input)` is the current in-memory `pr.finalize` tolerance bridge; wraps `validateStepResult` with `prFinalizeResultSchema` and `PR_FINALIZE_SCHEMA_ID`; default normalizer is `prFinalizeCleanResultNormalizer`; maps unrecoverable validation to `pr_finalize_invalid_result`; called by `DefaultOrchestrator` at `pr.finalize` result consumption — `parsePullRequestFinalizeResult` must not be used as the AI-step tolerance boundary. `src/spec-freeze.ts` — `freezeRunSpecForPullRequest(input, deps)` updates spec frontmatter status to `complete` and commits via `RunWorkspaceGitPort`; called by the orchestrator between `pr.finalize` (advance) and `pr.open`. `src/pr-open-handler.ts` — `handlePullRequestOpen(runId, tenant, deps)` is the idempotent PR creation system handler; performs a full `findByBranch` recovery decision table (checks provider before create; handles unknown outcome and persistence failure separately); emits `runner_notification` event with safe PR facts after creation; exports `PullRequestOpenHandlerDependencies` interface. **Issue 84 PR fallback content:** `src/convergence-engine.ts` and `src/layered-convergence-engine.ts` persist `ConvergenceRoundRecord.changedFilePaths` from `RunWorkspaceCommitResult.changedFilePaths`; `src/orchestrator.ts` uses `buildImplementationSummaryRoundInputs` so clean rounds no longer synthesize `Round N: implementation passed review` or count-only changed-file entries. `src/implementation-summary.ts` owns `mergeChangedFiles`, `summarizeChangedPaths`, and convergence-round-to-summary input mapping. `src/conventional-title.ts` can derive fallback subjects from changed paths when final-review fields and real summary text are absent. `src/pr-content.ts` renders only repository-relative changed paths and filters legacy count-only placeholders. `src/pr-lifecycle.ts` — `detectPullRequestMerges(input, deps)` drives bounded reconciliation (controlled by `maxCount` + `timeoutMs`): lists locally tracked open PRs, calls `codeHost.read` per PR, advances merged runs to `done`, fails closed (without merge) runs with `pull_request_closed_without_merge` error code; exports `PullRequestStatusReconciliationResult` interface. `src/pr-lifecycle.ts` — `detectPullRequestMerges(input, deps)` drives bounded reconciliation (controlled by `maxCount` + `timeoutMs`): lists locally tracked open PRs, calls `codeHost.read` per PR, advances merged runs to `done`, fails closed (without merge) runs with `pull_request_closed_without_merge` error code; exports `PullRequestStatusReconciliationResult` interface. **NEW — reconcile service surface:** `src/control-plane-service.ts` now also exports `reconcilePullRequests(principal)` on `ControlPlaneService` and `DefaultControlPlaneService`; delegates to `detectPullRequestMerges` and maps the result to `ReconcilePullRequestsResponse`; also exports `ServiceReconcilePullRequestsInput` and `ServiceReconcilePullRequestsResult` (aliases `ReconcilePullRequestsResponse` for B1 — see `decisions/pr-reconciliation-service-result-coupling.md`). `src/routes.ts` registers the protected `POST /v1/pull-requests/reconcile` route with bearer auth; dispatches to `controlPlaneService.reconcilePullRequests(principal)`; uses `pullRequestReconciliationPath` and `reconcilePullRequestsSuccessStatusCode` from `@autocatalyst/api-contract`. **Pull-request persistence additions:** `PullRequestRepository` (in `src/domain-repositories.ts`) now has `updateState(input: UpdatePullRequestStateInput)` and `listOpen(input: ListOpenPullRequestsInput)` — implemented by `DrizzlePullRequestRepository` in `packages/persistence/`. **B1 lifecycle semantics:** `pr.human_review` is the external code-host review/merge-detection waiting step — NOT a rich in-product merge UI; the run stays at `pr.human_review` until `detectPullRequestMerges` observes a provider state change. **NEW — issue tracker port and intake resolver (issue reference feature):** `src/issue-tracker.ts` — provider-neutral tracker port: `IssueTrackerPort` interface (`read(input): Promise<TrackedIssue>`), `IssueTrackerTarget` (provider, repository owner/name, credentialRef), `ReadTrackedIssueInput`, `IssueTrackerErrorCode` enum (`issue_not_found|tracker_auth_failed|tracker_credential_missing|tracker_provider_unavailable|tracker_target_invalid|tracker_response_invalid`), `IssueTrackerError` class. No GitHub/git/workspace mentions. `src/issue-tracker-registry.ts` — `IssueTrackerRegistry` interface and `StaticIssueTrackerRegistry` (normalizes provider names lowercase+trim at construction and lookup; `get(provider)` returns `IssueTrackerPort | null`). `src/issue-reference-intake.ts` — `IssueReferenceIntakeResolver` seam, `DefaultIssueReferenceIntakeResolver`: extracts issue numbers from free-form text via two regexes (`/\bissue\s+#?(\d+)\b/gi` and `/#(\d+)\b/g`), deduplicates across both; resolves credential via `issueTrackerSetting.credentialRef` (preferred) or exactly one `credentialRefs` entry with `purpose==='issue_tracker'`; settles work kind from normalized labels first (`feature|enhancement|bug|chore`), then title prefix fallback (`feat:`→`feature`, `fix:`→`bug`, `chore:`→`chore`); wraps `IssueTrackerError` as `IssueReferenceIntakeError('tracker_read_failed')` with only safe `trackerCode`; `IssueReferenceIntakeErrorCode` enum: `no_issue_numbers_found|multiple_issue_numbers_found|unsupported_tracker_provider|tracker_credential_missing|work_kind_undetermined|tracker_read_failed|intake_configuration_invalid`. `DefaultControlPlaneService` now accepts `projects: ProjectRepository` and `issueReferenceIntakeResolver: IssueReferenceIntakeResolver`; maps `IssueReferenceIntakeError` to `ControlPlaneServiceError('intake_routing_error')`. Public entry point: `packages/core/src/index.ts`. It may import the execution package through `@autocatalyst/execution` but must not import execution internals.
- `packages/execution/` — execution-plane package. `src/failure-reasons.ts` preserves public-safe reviewer/convergence reasons (`reviewer_result_missing`, `reviewer_result_invalid`, `disposition_missing`, `disposition_invalid`) and `transient_provider_failure`; unknown raw strings still normalize to `runner_failed_before_terminal_result`. `src/connection.ts` maps exhausted transient retries to `ProviderConnectionError('transient_provider_failure')`. `src/loopback-proxy.ts` buffers replayable request bodies up to a bounded cap and retries transient upstream status codes / pre-header transport errors with bounded `Retry-After` or exponential backoff; it does not retry after response streaming reaches the client. `src/errors.ts` owns `ClassifiedProviderFailureError`. HTTP 401 and auth-shaped provider errors classify to `provider_auth_failed` without copying raw SDK messages, bodies, headers, tokens, paths, URLs, or secret handles. Public entry point: `src/index.ts`, which exports the runner scaffold plus workspace provisioning, prune, and teardown APIs from `src/workspace.ts`; also exports skill resolver symbols: `resolveSkills`, `validateSkillCatalog`, `resolveCatalogAssetPath`, `SkillCatalogResolutionError`, `runtimeSkillsCatalog`, `runtimeSkillsCatalogRoot`. **Skills catalog and resolver:** `src/skills/catalog.ts` — runtime skills catalog: `runtimeSkillsCatalog` (record of B1 catalog entries keyed by skill name, e.g. `'mm:planning'`, `'mm:writing-guidelines'`), `runtimeSkillsCatalogRoot` (absolute path to the vendored assets directory). `src/skills/skill-resolver.ts` — `validateSkillCatalog(catalog)` validates catalog structure at startup; `resolveSkills(refs, catalog)` performs transitive resolution of skill references against the catalog and returns `ResolvedSkill[]`; `resolveCatalogAssetPath(catalogRoot, relativePath)` resolves and validates path containment for catalog assets; `SkillCatalogResolutionError` with typed codes (`skill_not_found`, `catalog_asset_path_traversal`, `catalog_invalid`). `src/skills/assets/` — vendored B1 skill assets: `mm:planning/` and `mm:writing-guidelines/` subdirectories each contain the skill definition files and a `SOURCE.json` provenance metadata file recording the upstream source and pinned version. (`provisionWorkspace`, `pruneWorkspacePath`, `teardownWorkspace`, lifecycle result/error types, and redacted workspace diagnostics), plus `createExecutionMaterializer` / `ExecutionMaterializer` from `src/internal/execution-materializer.ts`. Runner boundary: `src/runner.ts` owns the `Runner` interface (`run(input): AsyncIterable<RunnerEvent>`, `close(): Promise<RunnerCloseResult>`), `RunnerRunInput`, `RunnerCloseResult`, and `RunnerProtocolError` with codes: `invalid_event`, `wrong_run`, `duplicate_terminal_result`, `event_after_terminal`, `missing_terminal_result`, `runner_failed`, `runner_close_failed`. Materialized environment: `src/materialized-environment.ts` owns `MaterializedWorkspace` (none/scratch_only/two_roots discriminated union with `workspaceRoots`), `MaterializedExecutionEnvironment` (context, workspace, environment variables, toolPolicy, skills, capabilities), and `ExecutionMaterializationError` with codes: `missing_workspace_settings`, `workspace_provisioning_failed`, `secret_resolution_failed`, `unsupported_workspace_shape`, `capability_materialization_failed`. Secret resolver seam: `src/secret-resolver.ts` owns `ExecutionSecretResolver` interface (structurally compatible with core's `SecretResolver` but no import from `@autocatalyst/core`) and the `sanitizeSecretResolutionCause` helper. `src/execution-entry-point.ts` owns `createExecutionEntryPoint({ runner, materialize, resultValidation })` — `resultValidation` is required and accepts `NoExecutionResultValidationConfig` (`mode: 'none'`) or `ScratchFileExecutionResultValidationConfig` (`mode: 'scratch_file'`, requires `schema`+`schemaId`, or `contract`, or `contractRegistry`+`step`+`schemaId`); `runner.close()` is called in a finally block; validation failures synthesized as `fail` terminal with sanitized reason (`Execution failed: <code>`); `runner_close_failed` is thrown only when the stream completed cleanly AND a terminal event was seen. `src/execution-boundary-events.ts` owns `ExecutionTerminalResultEvent` (post-validation terminal with `result: RunnerTerminalStepResult` from api-contract handoff schema), `ExecutionBoundaryEvent` union (non-terminal `RunnerEvent` | `ExecutionTerminalResultEvent`), `validateExecutionBoundaryEvent`, and `validateExecutionBoundaryEventStream` — raw `RunnerEvent` validation stays in entry-point; this module validates the converted stream consumed by core. Step result tolerance pipeline: `src/result-contracts.ts` — exact `(step, schemaId)` registry via `createStepResultContractRegistry`; exports `SPEC_AUTHOR_SCHEMA_ID` (`autocatalyst.spec_author.v1`), `SYSTEM_SPEC_AUTHOR_SPECCED_BY = 'autocatalyst'`, and `REVIEWER_RESULT_SCHEMA_ID = 'autocatalyst.reviewer_result.v1'` constants; `registerSpecAuthorResultContract()` wraps `specAuthorResultSchema` in `z.preprocess` to stamp `specced_by: 'autocatalyst'` before validation via `stampSpecAuthorResultIdentity` helper — this ensures the model never needs to supply `specced_by`; `registerReviewerResultContract()` registers `reviewerResultSchema` for the `implementation.build` step; `resolveStepResultContract` never falls back to generic schemas, returns `result_contract_unknown` for misses; also exports `PR_FINALIZE_SCHEMA_ID = 'autocatalyst.pr_finalize.v1'`, `PullRequestFinalizeResultContractOptions`, `createPullRequestFinalizeResultContract(options?)` (default normalizer: `prFinalizeCleanResultNormalizer`), and `registerPullRequestFinalizeResultContract(registry, options?)`; `StepResultContractDefinition` now carries optional `normalizers`, `correctionRequester`, `maxCorrectionAttempts`, and `degradationPolicy` fields consumed by the execution entry point; **`src/execution-entry-point.ts`** — `createExecutionEntryPoint` now accepts a `contractRegistry`+`step`+`schemaId` validation config variant; the entry point uses it to validate scratch output before core receives `advance.result`; registered via `registerSpecAuthorResultContract()` for `spec.author` runs and `registerReviewerResultContract()` for `implementation.build` reviewer runs. When both contract-local normalizers and config normalizers are present, the entry point composes contract normalizers before config normalizers via `composeNormalizers`; config correction/max-attempt/degradation policy explicitly overrides contract policy when set. **Updated — request alteration:** `src/request-alteration.ts` — fixed: `ANTHROPIC_CUSTOM_HEADERS` is now added to `secretVariableNames` when `headersToRewrite` is configured, ensuring custom header values are redacted in launch logs; serializes `ANTHROPIC_CUSTOM_HEADERS` for Claude process launch as newline-delimited `Name: Value` lines, redacts the whole env var when secret-bearing, and caps request timeouts at 600000 ms. `src/proxy-header-policy.ts` preserves request `content-length` while still stripping true hop-by-hop headers. Tests: `src/request-alteration.spec.ts`. `src/result-tolerance.ts` — ADR-012 pipeline: normalization → validation → bounded correction → optional degradation metadata; `validateStepResult` returns `{ status: 'ok', value }` or `{ status: 'failed', code }`; error codes: `schema_validation_failed`, `correction_exhausted`, `normalizer_failed`, `correction_requester_error`; `ResultDegradationPolicy` allows `'allow_degraded'` to pass partially-invalid results; `src/result-normalizers.ts` — deterministic normalizer registration, records ambiguous input as telemetry; `ResultNormalizerRegistry` maps `(schemaId) → ResultNormalizer[]`; `createSpecAuthorFrontmatterNormalizer(options?)` is schema-id gated to `SPEC_AUTHOR_SCHEMA_ID` — drops system-owned keys (`created`, `last_updated`, `status`, `issue`, `specced_by`) and non-allowlist keys (`implemented_by`, `supersedes`, `superseded_by` are allowed), drops null optional values, then stamps system-owned fields via `stampSpecAuthorResultIdentity`; `prFinalizeCleanResultNormalizer` is schema-id gated to `PR_FINALIZE_SCHEMA_ID` — maps `{}`, `{findings:[]}`, and `{validationSummary:[]}` to clean advance, leaves all other candidates unchanged; `defaultResultNormalizers` remains `[reviewerResultNormalizer]` only — spec-author and PR-finalize normalizers are installed through contract factories, never added to the default list; `src/result-correction.ts` — provider-neutral `ResultCorrectionRequester` seam (`requestCorrection(candidate, errors) → Promise<unknown>`); `src/result-file.ts` — reads JSON result files only from `scratchRoot` (via `readScratchStepResultFile`), rejects path traversal out of scratch root; error codes: `result_file_missing`, `result_file_not_found`, `result_file_not_json`. Stub runner: `src/stub-runner.ts` owns `StubRunner` class with injectable `clock`, `eventIdGenerator`, `terminalResult`, `resultFile` (writes JSON to `scratchRoot` before terminal), `correctionResponses` (queue for `ResultCorrectionRequester`); emits exactly 4 events in order (runner_progress, runner_assistant_turn, runner_step_checkpoint, runner_terminal_result); reads `environment.context.run.id` and `currentStep`; checkpoint payload includes `workspaceShape`, `workspaceRootCount`, `shellAvailable`, `lspAvailable`; no network/SDK calls; `close()` returns `{ status: 'closed' }`. **Runner connection layer**: `src/request-alteration.ts` — fetch alteration (`applyRequestAlteration`) applying URL rebase, header strip/rewrite, auth injection, timeout/retry clamping; process launch env mapping (`buildClaudeProcessLaunchEnvironment`) that overlays Claude-owned env vars from endpoint settings, maps credential to `authEnvironmentVariable` (default `ANTHROPIC_API_KEY`), encodes custom headers as `ANTHROPIC_CUSTOM_HEADERS` JSON; structured redaction helpers (`redactProviderRequestForLog`, `redactProviderResponseForLog`, `redactProcessLaunchConfigForLog`); and `isTransientProviderFailure` for HTTP status / transport-error detection. `src/agent-provider-adapter.ts` — public adapter contract types: `AgentProviderAdapter`, `AgentConnection`, `ResolvedAgentRunnerProfile`, `ResolvedAgentCredentialReference`, `ProviderFetchTransport`, `ProcessLaunchConfig`, `ProcessLaunchConfigInput`, `AgentProviderSessionInput`, `AgentProviderSession`, `AgentProviderSessionMetadata`, `AgentTokenUsage`; also `ProviderConfigurationError`, `ProviderConnectionError`, `ProviderProtocolError`, and `UnsupportedProviderCapabilityError` (all with typed codes, never include raw upstream secrets). `src/connection.ts` — `createAgentConnection(options)` factory: resolves credential eagerly from secret store, validates base URL for fetch_transport profiles, and returns an `AgentConnection` handle whose `createFetchTransport()` method executes the retry loop with timeout, redacted logging, and transient-failure retries; `createProcessLaunchConfig(input)` delegates to `buildClaudeProcessLaunchEnvironment` and redacts the result for logs — credential stays in closure and is never stored on the handle. **Loopback runner proxy (new):** `src/loopback-proxy.ts` — 127.0.0.1 random-port lifecycle, one-shot upstream forwarding, streaming backpressure, safe 502 envelopes, request counts, shutdown, request/response dump wiring. `src/proxy-header-policy.ts` — URL mapping (`mapLoopbackUrlToUpstream`) and proxy-mode header policy (hop-by-hop stripping, endpoint strip, token filters, additive rewrites, auth injection). `src/proxy-request-logging.ts` — default-off diagnostic dump logger (0o700 dir, 0o600 files, capped 64 KiB capture, symlink/traversal protection, graceful disablement). `src/proxy-redaction.ts` — shared redaction for proxy dumps and fetch log-shape alignment. `src/connection.ts` now selects proxy mode from endpoint `proxyMode`/`headersToStrip`/`proxyRequestLogging`/`headerValueFilters`, lazily starts one proxy per connection, closes it from `AgentConnection.close()`, and makes `createProcessLaunchConfig` async. Phase 1 stopgap: `buildClaudeProcessLaunchEnvironment` merges `authHeaderName` credential into `ANTHROPIC_CUSTOM_HEADERS` for subprocess auth. Proxy targeted commands: `pnpm nx test execution -- proxy-header-policy.spec.ts`, `pnpm nx test execution -- proxy-request-logging.spec.ts`, `pnpm nx test execution -- loopback-proxy.spec.ts`, `pnpm nx test execution -- connection.spec.ts`. `src/agent-orchestrator-runner.ts` — `createAgentOrchestratorRunner(options)` factory that wraps any `AgentProviderAdapter` into the `Runner` interface; validates adapter/profile providerKind and connectionMechanism match before first yield; drives `adapter.startSession`, enforces single-terminal protocol and no-event-after-terminal rules, emits telemetry events at session start/end with token usage and degraded-capability summary. `src/runner-dispatch.ts` — `createAgentRunnerFactory(options)` lookup-based runner factory: resolves profile via `resolveProfile` seam, looks up adapter from `AgentProviderAdapterRegistry` by `getAgentProviderAdapterKey(providerKind, adapterId)`, validates providerKind/connectionMechanism match, creates connection, and returns an `AgentOrchestratorRunner`; also exports `AgentRunnerFactory`, `AgentRunnerFactoryInput`, `AgentProfileResolution`, `AgentProviderAdapterRegistry`, and `CreateAgentRunnerFactoryOptions`. **Direct-mode (bounded-call port — NOT a streaming Runner):** Direct mode is a single bounded call, not an event stream. `DirectCallFactory.call()` returns a `DirectOrchestratorCallResult` with a validated `value`. Provider adapter packages (`anthropic-direct-adapter`, `openai-direct-adapter`, `claude-agent-adapter`) must import ONLY from `@autocatalyst/execution` public API, never from `@autocatalyst/execution/src/*`. Control-plane registers cells through adapter factories and registries: the Claude agent cell uses `agentRegistry`; the Anthropic direct and OpenAI direct cells use `directRegistry` via `createDirectCallFactory()`. `src/direct-provider-adapter.ts` — provider-neutral direct adapter contract. Defines `DirectProviderAdapter`, `DirectCallRequest`, `DirectProviderCallInput`, `DirectProviderCallResult`, `DirectProviderCallMetadata`, `DirectProviderProtocolError`, and `DirectResultValidationError`. No provider-specific names. `src/direct-orchestrator.ts` — `createDirectOrchestrator()`. Bounded-call port for direct invocations. Validates adapter/profile compatibility, calls adapter once, validates result via `validateStepResult`, emits telemetry. Provider packages must not import this directly — use `@autocatalyst/execution` public API. `src/direct-runner-dispatch.ts` — `createDirectCallFactory()` and `createDirectProviderAdapterRegistry()`. Lookup-based direct adapter selection by `(providerKind, adapterId, mode)`. No provider-specific branches. Internal workspace modules are package-private: `src/internal/workspace-paths.ts` owns shape selection, safe segment validation, path resolution, containment checks including no-final-symlink-resolution prune containment, and branch-name derivation; `src/internal/workspace-driver.ts` owns argument-array git commands, host clone/fetch/default-branch/worktree operations, stat classification, checkpoint/branch operations, credential redaction, and guarded filesystem operations; `src/internal/workspace-pruner.ts` is the single policy-level gateway for workspace filesystem deletion and git worktree removal; `src/internal/workspace-teardown.ts` applies terminal-state retention policy; `src/internal/workspace-provisioner.ts` orchestrates no-workspace, scratch-only, and two-root provisioning plus pruner-mediated rollback; `src/internal/workspace-logger.ts` provides the injectable structured logging seam used by pruner and teardown; `src/internal/execution-materializer.ts` owns `createExecutionMaterializer(options)` with injectable `provisionWorkspace` and `secretResolver` — resolves a declarative `ExecutionContext` into a `MaterializedExecutionEnvironment`, never reads `process.env`; workspace provisioning failures use a static message with `summarizeWorkspaceCause(error)` in `details` only (not the exception message) so raw error text is never propagated to persisted failure reasons; `src/internal/workspace-root-guard.ts` owns `assertPathWithinWorkspaceRoots(candidatePath, workspaceRoots)` using `node:path` resolve()/relative() containment — always throws `RunnerProtocolError('runner_failed', 'Path is outside materialized workspace roots.')` for out-of-bounds paths. Tests: `src/execution-materializer.spec.ts`, `src/execution-entry-point.spec.ts`, `src/result-tolerance.spec.ts`, `src/result-contracts.spec.ts`, `src/result-file.spec.ts`, `src/workspace-paths.spec.ts`, `src/workspace-provisioner.spec.ts`, `src/workspace-pruner.spec.ts`, `src/workspace-teardown.spec.ts`, `src/workspace.integration.spec.ts`, `src/workspace-teardown.integration.spec.ts`. Useful targeted commands: `pnpm nx test execution -- execution-entry-point.spec.ts`, `pnpm nx test execution -- result-tolerance.spec.ts`, `pnpm nx build execution`, `pnpm nx lint execution`.
- `packages/persistence/` — persistence package. `src/active-run-conflict.ts` owns the typed `ActiveRunConflictPersistenceError` and the `isActiveRunConstraintViolation` recogniser used to translate raw SQLite unique-index errors into the typed conflict. `DrizzleConversationIngressRepository` (in `src/domain-repositories.ts`) implements the `ConversationIngressRepository` seam: `createConversationTopicMessageAndRun` writes conversation+topic+(optional)message+run+initial-runStep atomically in one transaction, sets `activeTopicId` on the conversation, and maps the `runs_one_active_per_topic` constraint violation to `ActiveRunConflictPersistenceError`. `src/sqlite.ts` owns the opaque SQLite handle, migrations, and reachability; `src/schema.ts` is the internal Drizzle schema — control-plane tables: `configurationRecords`, `secretStoreMetadata`, `secrets` (added in migration `0001`); domain tables added in migration `0002`: `projects`, `conversations`, `topics`, `messages`, `runs`, `artifacts`, `feedback`, `publications`, `pullRequests` (SQL: `pull_requests`), `runSteps` (SQL: `run_steps`), `sessions`, `testResults` (SQL: `test_results`); key constraints: `runs_one_active_per_topic` partial unique (WHERE `terminal=0`), `topics_one_main_per_conversation` partial unique (WHERE `kind='main'`), `pull_requests_one_per_run` unique; `src/probe-resource-repository.ts` implements the core probe-resource repository; `src/configuration-record-repository.ts` implements `ConfigurationRecordRepository` (`DrizzleConfigurationRecordRepository`, IDs prefixed `cfg_<uuid>`, settings serialized as JSON, PATCH uses read-modify-write, `credentialSecretHandle: null` removes the key); provider-profile PATCH persists `model`, `inferenceSettings`, and `endpoint` fields including null-as-clear semantics; scopes all operations by `tenant`; enforces at most one active `model_routing_table` per tenant on create/update (unique partial constraint); supports kind-tagged create/update with merge semantics for routing-table settings; `src/secret-store.ts` implements both `SecretStore` and `SecretResolver` (`SqliteSecretStore`, AES-256-GCM encryption, scrypt KDF N=16384, sentinel row for unlock verification, injectable `randomBytes` for test isolation, handle format `sec_` + 32 base64url chars; `resolveSecret(handle)` decrypts and returns plaintext, throws `SecretResolutionError` for `locked`/`missing_secret`/`undecryptable` states); `src/domain-row-mappers.ts` — `stringifyJsonValue`, `parseJsonValue`, `parseNullableJsonValue`, `nullableJsonForRow`, `validateEntity`; `src/domain-repositories.ts` — 12 Drizzle repository implementations (`DrizzleProjectRepository`, etc.), `DrizzleDomainRepositories` interface, and `createDrizzleDomainRepositories` factory; also implements lifecycle-owned `DrizzleRunRepository.recordRunLifecycleStart` and `recordRunStepTransition`, which atomically write run state and matching `RunStep` occurrence rows with transaction-computed `index` and `attempt` values; `src/domain-repositories.ts` also adds feedback status/thread atomic transitions (`addressFeedback`, `resolveFeedback`, `wontFixFeedback`, `reopenFeedback`) plus Artifact run-kind lookup (`getArtifactRunKind`) and cached-status update (`updateArtifactCachedStatus`); **NEW** `DrizzleRunStepRepository.updateCheckpoint()` — atomic convergence-checkpoint persistence: validates the payload against `convergenceCheckpointSchema` before write, stores in `runSteps.checkpointResult`. Migration `0008_run_failure_reason.sql` adds nullable `runs.failure_reason`; `DrizzleRunRepository.recordRunStepTransition` stores it for failed transitions and clears it for non-failed target states. Committed migrations live under `packages/persistence/drizzle/` (migration `0002_even_victor_mancha.sql` creates all 12 domain tables). Tests: `__tests__/domain-migrations.spec.ts`, `__tests__/domain-repositories.spec.ts`, `__tests__/domain-active-run-constraint.spec.ts`. Public entry point: `packages/persistence/src/index.ts`.
- `packages/openai-agent-adapter/` — `@autocatalyst/openai-agent-adapter`, the `{openai, agent}` runner cell, integrated with the **real** `@openai/agents` SDK (0.11.6; deps: `@openai/agents`, `openai`, `zod`@4 — the adapter pins zod 4 for the SDK while the rest of the repo stays on zod 3.25, see `decisions/openai-agent-adapter-sdk-integration.md`). Implements `AgentProviderAdapter` with `providerKind='openai'`, `adapterId='openai-agents-sdk'`, `supportedConnectionMechanism='fetch_transport'`.
  - **Per-session binding, never global.** `startSession` builds a per-session `OpenAI` client whose `fetch` is bridged to `connection.createFetchTransport()` (`bridgeTransportToFetch`), wraps it in `new OpenAIProvider({ openAIClient, useResponses: false })`, and hands that to a per-session `new Runner({ modelProvider, tracingDisabled: true })`. The SDK's `setDefaultModelProvider`/`setDefaultOpenAIClient`/`setOpenAIAPI` globals are never called (a source-scan test enforces this).
  - **Sandbox + workspace materialization.** `UnixLocalSandboxClient({ workspaceBaseDir, snapshot })` is created per session with an explicit `NoopSnapshotSpec` (validated via `isNoopSnapshotSpec`; type must be `'noop'` or `sandbox_snapshot_unsupported` is thrown). Each declared workspace root becomes a `localDir({ src: root })` `Manifest` entry **plus** an `extraPathGrants` entry (the local sandbox otherwise rejects `local_dir` sources outside its base dir). `client.create(manifest)` opens the session; `runner.run(agent, prompt, { sandbox: { session } })` drives it. Workspace roots that escape the declared set fail with `workspace_containment_violation`.
  - **Event mapping.** The run driver yields the SDK's `RunItem`s; `mapRunItem` maps `message_output_item`→`runner_assistant_turn`, `tool_call_item`→`runner_tool_activity` (or progress/notification for `update_plan`/`report_progress`/`notify`), `tool_call_output_item`→`runner_tool_activity` result. A single terminal `runner_terminal_result` is synthesized from `result.finalOutput`/directive after the item stream (terminal protocol: exactly one, always last). `finalOutput` is written to `<scratchRoot>/step-result.json` (validation left to the execution entry point); tokens come from `state._context.usage`.
  - **Seams.** Two injectable options default to the real SDK: `sandboxClientFactory` (returns `{ client, session, close? }`) and `runAgentSession` (returns `{ items, result }`). Tests inject fakes for dispatch wiring; the package's own spec drives the **real** module with only the OpenAI client's `fetch` mocked. `sandboxWorkspaceBaseDir` points the default local sandbox at a temp dir.
  - **Errors** use sandbox/snapshot/containment-specific `UnsupportedProviderCapabilityErrorCode`s added in `packages/execution/src/agent-provider-adapter.ts`: `sandbox_client_unsupported`, `sandbox_snapshot_unsupported`, `workspace_containment_violation` (replacing the previous misuse of `tool_policy_unsupported`).
  - **Skill materialization:** `src/skill-materialization.ts` — `materializeOpenAISkillFiles(resolvedSkills, stagingDir)` stages resolved skills as sandbox filesystem mounts: copies each skill asset tree into a temporary staging directory, writes a `skill-discovery-manifest.json` alongside it listing all staged skill paths, and returns an `OpenAISkillMaterialization` (`{ stagedPaths: string[], manifestPath: string }`) consumed by the adapter when configuring the sandbox session.
  - Provider SDK auth failures are converted to `ClassifiedProviderFailureError('provider_auth_failed')`; logs include safe metadata such as provider kind, adapter id, status/code/errorName, and never `err.message` or raw response/request detail. Unsupported or unknown provider failures fall back to safe generic reasons rather than exposing diagnostics.
  - Public exports: `createOpenAIAgentAdapter`, `openaiProviderKind`, `openaiAgentAdapterId`, and seam types (`OpenAIAgentAdapterOptions`, `OpenAISandboxClientFactory`, `OpenAISandboxClientFactoryInput`, `OpenAISandboxClientHandle`, `OpenAISandboxSession`, `OpenAIRunAgentSession`, `OpenAIRunSessionInput`, `OpenAIRunOutcome`, `OpenAIWorkspaceSandboxConfig`, `OpenAIAgentAdapterLogger`).
  - Tests: `packages/openai-agent-adapter/src/openai-agent-adapter.spec.ts` (16 tests incl. the real-module fetch-mocked integration test), `apps/control-plane/src/runner-cells.integration.spec.ts`, `apps/control-plane/src/integration.spec.ts`. Commands: `pnpm nx test openai-agent-adapter`, `pnpm nx test control-plane`.
- `packages/openai-direct-adapter/` — `@autocatalyst/openai-direct-adapter`. The `{openai, direct}` cell. Implements `DirectProviderAdapter` (`providerKind='openai'`, `adapterId='openai-direct'`, `supportedConnectionMechanism='fetch_transport'`), mirroring `anthropic-direct-adapter` in shape. `createOpenAIDirectAdapter(options?)` makes a single bounded `POST {baseUrl}/v1/chat/completions` call through `connection.createFetchTransport()` — no OpenAI Agents SDK. Forces structured output via OpenAI function calling (`tools` + `tool_choice` selecting `autocatalyst_direct_result`); extracts exactly one tool_call's `function.arguments` as the candidate, with a JSON-in-`message.content` fallback. Same protocol-error discipline as the Anthropic direct adapter (`structured_result_missing`, `structured_result_malformed`, `multiple_structured_candidates`, `extra_structured_output`); transport throw → `ProviderConnectionError('timeout')`, non-ok → `ProviderConnectionError('non_transient_provider_failure')`. Maps OpenAI usage (`prompt_tokens`→input, `completion_tokens`→output, cache fields zeroed); tracks `topK`/`streamingMode`/`parallelToolCalls` as `degradedCapabilities`. Registered on the control-plane direct registry alongside the Anthropic direct cell. NOTE: the `{openai, agent}` cell is a separate future package, not this one. Must import only from `@autocatalyst/execution` public API, never from internal paths.
- `packages/anthropic-direct-adapter/` — `@autocatalyst/anthropic-direct-adapter`. Implements `DirectProviderAdapter` for direct (bounded-call) Anthropic invocations. Preferred output: `tool_use` structured output via `autocatalyst_direct_result`; fallback: JSON-only text. Uses `connection.createFetchTransport()`. Must import only from `@autocatalyst/execution` public API, never from internal paths.
- `packages/github-code-host-adapter/` — `@autocatalyst/github-code-host-adapter`. Implements `CodeHostPort` (from `@autocatalyst/core`) for GitHub pull-request operations via the `gh` CLI. Key module: `src/github-code-host.ts` — `createGitHubCodeHostAdapter({ executeGh, git })` factory: implements `create` (`gh pr create`), `read` (`gh pr view`), `findByBranch` (`gh pr list --head`), `update` (`gh pr edit`), and `merge` (`gh pr merge`); uses the safe `executeGh` seam from `github-issue-tracker-adapter` (no raw subprocess in core); only squash+delete-branch merge strategy is supported — other strategies throw `merge_strategy_unsupported`; all error paths map to typed `CodeHostError` codes. Exported from `packages/github-code-host-adapter/src/index.ts` via `export * from './github-code-host.js'`.
- `packages/github-issue-tracker-adapter/` — `@autocatalyst/github-issue-tracker-adapter`. Implements `IssueTrackerPort` (from `@autocatalyst/core`) for GitHub issue reads via the `gh` CLI subprocess. Tags: `type:lib`, `scope:adapter`, `plane:control`. Key modules: `src/gh-exec.ts` — safe `execFile`-based `gh` subprocess helper; token injected via `GH_TOKEN` env, never in CLI args; stdout bounded at 1 MB; error codes: `gh_not_found`, `gh_auth_failed`, `gh_resource_not_found`, `gh_provider_unavailable`, `gh_timeout`, `gh_error`; token/stderr never appear in thrown errors. `src/github-issue-tracker.ts` — `GitHubIssueTracker implements IssueTrackerPort`: validates `target.repository.owner/name` and `target.credentialRef` before exec; resolves token immediately before gh execution via `SecretResolver.resolveSecret`; calls `gh issue view <number> --repo <owner>/<name> --json number,title,body,labels,state,url`; normalizes GitHub uppercase state (`OPEN`→`open`), label objects `{name}` → string, null body → `""`; maps `GhExecError` codes to `IssueTrackerError` codes safely. `src/github-issue-tracker.live.spec.ts` — opt-in live proof skipped unless `AUTOCATALYST_LIVE_GITHUB_ISSUE_READ=1`, `AUTOCATALYST_LIVE_GH_REPO`, `AUTOCATALYST_LIVE_GH_ISSUE_NUMBER`, and `AUTOCATALYST_LIVE_GH_TOKEN` are set. Public exports from `src/index.ts`: `executeGh`, `GhExecError`, `GhExecErrorCode`, `GhExecInput`, `GhExecResult`, `GitHubIssueTracker`, `GitHubIssueTrackerOptions`. Test command: `pnpm nx test github-issue-tracker-adapter`. Live opt-in: `AUTOCATALYST_LIVE_GITHUB_ISSUE_READ=1 AUTOCATALYST_LIVE_GH_REPO=owner/name AUTOCATALYST_LIVE_GH_ISSUE_NUMBER=71 AUTOCATALYST_LIVE_GH_TOKEN=ghp_... pnpm nx test github-issue-tracker-adapter -- github-issue-tracker.live.spec`.
- `packages/claude-agent-adapter/` — Claude Agent SDK adapter package (`@autocatalyst/claude-agent-adapter`). Implements `AgentProviderAdapter` for `providerKind='anthropic'` / `adapterId='claude-agent-sdk'` with `supportedConnectionMechanism='process_environment'` only. `src/claude-agent-adapter.ts` owns `createClaudeAgentAdapter(options?)` factory: evaluates inference-setting capabilities against `DEFAULT_UNSUPPORTED_INFERENCE_KEYS` (throws `UnsupportedProviderCapabilityError` for required unsupported settings, records degradations otherwise); calls `connection.createProcessLaunchConfig` to obtain the env overlay; resolves `cwd` from materialized workspace (`scratchRoot` preferred over `workspaceRoots[0]`); maps native SDK events to `RunnerEvent` via `mapNativeEvent` — `assistant` → `runner_assistant_turn`, `tool_use` on progress tools (`update_plan`, `report_progress`, `notify`) → `runner_progress`/`runner_notification`, other `tool_use` → `runner_tool_activity`, `result` → captured as `pendingResult` with token usage; after the native stream ends, writes `step-result.json` to `scratchRoot` (if output present) and emits `runner_terminal_result(advance)`; all string content is scrubbed of known-secret values via inline redaction; `metadata` promise resolves on success, rejects on failure — never includes raw error messages. **Skill materialization:** `src/skill-materialization.ts` — `materializeClaudeSkillPlugins(resolvedSkills)` maps resolved skills to Claude SDK plugin entries (`{ type: 'claudecode', path }`) consumed by the adapter when launching the Claude process; one plugin entry per resolved skill. Neutral tool categories are translated before SDK launch (`bash` → `Bash`, `filesystem` → `Read`/`Write`/`Edit`/`Glob`/`Grep`, `lsp` omitted because Claude has no mapped LSP tool). The adapter does not overwrite an existing scratch `step-result.json`; if the agent wrote the contract file, that file remains the source of truth for execution validation. Provider SDK auth failures are converted to `ClassifiedProviderFailureError('provider_auth_failed')`; logs include safe metadata such as provider kind, adapter id, status/code/errorName, and never `err.message` or raw response/request detail. Unsupported or unknown provider failures fall back to safe generic reasons rather than exposing diagnostics. Public entry point `src/index.ts` exports `createClaudeAgentAdapter`, `claudeProviderKind`, `claudeAgentAdapterId`, and the options/logger/launch seam types. Tests: `src/claude-agent-adapter.spec.ts`. Test command: `pnpm nx test claude-agent-adapter`.
- `packages/sdk/` — SDK package. `src/client.ts` exposes typed health, probe-resource, configuration-record (`createConfigurationRecord`, `listConfigurationRecords`, `getConfigurationRecord`, `updateConfigurationRecord`, `deleteConfigurationRecord`), secret (`createSecret`), orchestrator ingress (`createConversationWithFirstRun`, `getRun`, `listRunSteps`, `subscribeRunEvents`), spec review surface (`getRunSpec(id)` → `RunSpecResponse`, `createRunFeedback(id, request)` → `Feedback`, `listRunFeedback(id)` → `RunFeedbackListResponse`), and **NEW** `replyToRun(id, request)` → `RunReplyResponse` (sends an approve/feedback/guidance reply to a paused run) and **NEW** `reconcilePullRequests()` → `ReconcilePullRequestsResponse` (triggers on-demand PR merge reconciliation via `POST /v1/pull-requests/reconcile`) calls derived from `@autocatalyst/api-contract`. Accepts optional `bearerToken` in `ControlPlaneClientOptions`. Public entry point: `packages/sdk/src/index.ts`.
- `tools/boundary-tests/` — committed lint-level boundary assertions. The invalid fixture is excluded from normal package lint and is checked only by `pnpm test:boundaries`. `assert-execution-boundary.mjs` — scans `packages/` and `apps/` for direct `@autocatalyst/execution/src/*` imports; must fail on the invalid fixture. `assert-gh-issue-read-boundary.mjs` — scans all non-spec `.ts` files under `packages/` and `apps/` and fails if `gh issue view` appears outside the two allowed paths (`packages/github-issue-tracker-adapter/src/gh-exec.ts` and its spec). `assert-gh-pr-boundary.mjs` — enforces that `gh pr` calls (create/view/list/edit/merge) appear only in `github-code-host-adapter` and `github-issue-tracker-adapter`; prevents accidental direct `gh pr` CLI calls from core or other packages. All three scripts are chained in `package.json` `test:boundaries`.

## Operator docs

- `docs/operators/real-dispatch.md` — startup guide for real runner dispatch: env vars, CLI flags, and a Grove-backed Claude Agent SDK provider-profile example with `authHeaderName: "api-key"` and `requestTimeoutMs: 600000`.

## Key entry points

- Root workspace metadata: `package.json`, `pnpm-workspace.yaml`, `.npmrc`.
- Nx task graph and cache defaults: `nx.json`.
- Shared TypeScript paths and compiler settings: `tsconfig.base.json`.
- Lint and module-boundary rules: `eslint.config.mjs`.
- Package project metadata and tags: `packages/*/project.json`.

## Build / test / run commands

Run these from the repository root on Node.js 22+:

```bash
pnpm install
pnpm nx show projects
pnpm nx run-many -t build
pnpm nx run-many -t lint
pnpm nx run-many -t test
pnpm test:boundaries
pnpm validate
```

`pnpm validate` runs build, lint, test, then the committed execution-boundary test.

## Control-plane service envelope commands

```bash
# Start the service
CONTROL_PLANE_BEARER_TOKEN=dev-token CONTROL_PLANE_MASTER_SECRET=dev-master-secret CONTROL_PLANE_PORT=3000 CONTROL_PLANE_DATABASE_PATH=.data/control-plane.sqlite pnpm nx serve control-plane

# Run tests
pnpm nx test control-plane
pnpm nx test api-contract
pnpm nx test core
pnpm nx test persistence
pnpm nx test sdk
pnpm nx test claude-agent-adapter

# Targeted orchestrator-ingress proofs
pnpm nx test core -- orchestrator.spec.ts
pnpm nx test control-plane -- control-plane-service.integration.spec.ts
pnpm nx test persistence -- domain-active-run-constraint.spec.ts
pnpm nx test control-plane -- integration.spec.ts

# Issue reference intake targeted tests
pnpm nx test api-contract -- domain-value-objects.spec conversation-ingress.spec
pnpm nx test core -- issue-tracker.spec issue-tracker-registry.spec issue-reference-intake.spec
pnpm nx test github-issue-tracker-adapter -- gh-exec.spec github-issue-tracker.spec index.spec
pnpm nx test github-issue-tracker-adapter                                # includes live spec (skipped unless opt-in)
pnpm nx test control-plane -- issue-reference-create.integration.spec
pnpm nx test persistence -- domain-repositories.spec
pnpm test:boundaries                                                      # includes assert-gh-issue-read-boundary.mjs
AUTOCATALYST_LIVE_GITHUB_ISSUE_READ=1 AUTOCATALYST_LIVE_GH_REPO=owner/name AUTOCATALYST_LIVE_GH_ISSUE_NUMBER=71 AUTOCATALYST_LIVE_GH_TOKEN=ghp_... pnpm nx test github-issue-tracker-adapter -- github-issue-tracker.live.spec

# Model routing targeted tests
pnpm nx test api-contract -- configuration-record.spec.ts
pnpm nx test persistence -- configuration-record-repository.spec.ts
pnpm nx test core -- model-routing-resolver.spec.ts
pnpm nx test control-plane -- model-routing.integration.spec.ts
pnpm test:boundaries

# Convergence loop targeted tests
pnpm nx test api-contract -- convergence.spec run-step.spec   # contract schema tests (altitude schemas + run-step)
pnpm nx test core -- convergence-engine.spec                  # engine unit tests (36 tests)
pnpm nx test core -- convergence-policy.spec                  # policy + altitude ladder tests (12 tests)
pnpm nx test core -- altitude-contract-validator.spec         # AST-based early-altitude validator (21 tests)
pnpm nx test core -- layered-finding-filter.spec              # altitude finding filter (9 tests)
pnpm nx test core -- build-contract-preservation.spec         # drift detector (15 tests)
pnpm nx test core -- layered-convergence-engine.spec          # ladder coordinator (13 tests; includes build_only depth test)
pnpm nx test core -- convergence-feedback.spec                # feedback mapping (15 tests)
pnpm nx test core -- orchestrator.spec                        # dispatch selection tests (74 tests)
pnpm nx test control-plane -- run-workspace-git-port.spec     # checkpoint ref tests (17 tests)
pnpm nx test control-plane -- implementation-build-convergence.smoke.spec  # deterministic smoke (5 tests: happy path, stall, full ladder, drift, contract violation)
AUTOCATALYST_LIVE_IMPLEMENTATION_CONVERGENCE=1 pnpm nx test control-plane -- implementation-build-convergence-live.spec  # opt-in live e2e (requires provider credentials)

# Generate future migrations from schema (do not run before adding new schema changes)
pnpm drizzle-kit generate --config packages/persistence/drizzle.config.ts

# Full validation
pnpm validate
```

Orchestrator service ingress routes (added by Story 7-9):

- `POST /v1/conversations` — create a conversation, topic, optional message, run, and initial run step atomically; returns the created `Conversation`/`Topic`/`Message?`/`Run`/`RunStep`. Body schema: `createConversationWithFirstRunRequestSchema` from `@autocatalyst/api-contract`.
- `GET /v1/runs/:id` — return the run for the authenticated tenant. 404 if missing; 403 if cross-tenant. Run response includes `waitingOn` field (derived at read time from run-step catalog).
- `GET /v1/runs/:id/steps` — list run-steps for a run ordered by `(startedAt, id)`. 404/403 like above.
- `GET /v1/runs/:id/events` — Server-Sent Events stream of `ClientRunEvent` frames for the given run+tenant. Streams full runner event vocabulary (assistant turns, tool activity, progress, notifications, step checkpoints, terminal results) plus state transitions. Route subscribes before replaying so no events are lost during reconnect; replayed events are written first, then live events from the subscription. Each frame has `event: <type>`, `id: <eventId>`, and `data: <json>` lines. `Last-Event-ID` header triggers replay-after-cursor; unknown or expired cursors return HTTP `409 Conflict` with JSON `{ error: { code: "run_event_replay_cursor_unknown" | "run_event_replay_cursor_expired", message, lastEventId } }` before any SSE bytes (no `: connected` comment). **Retained SSE replay (opt-in):** `?replay=retained` query parameter replays only the durable retained-event history when explicitly set; without this parameter, plain streams (with or without `Last-Event-ID`) are live-only — cursor-based reconnect is the only way to replay without `replay=retained`. Implementation: `routes.ts` parses `replay: z.enum(['retained']).optional()`, `control-plane-service.ts` `SubscribeRunEventsInput` accepts `replay?: 'retained'`, and `run-events.ts` `InMemoryRetainedRunEventStore.replayAfter` replays all retained events when `replay === 'retained'` and no cursor is given.

Spec review routes (added by spec-review API surface enhancement):

- `GET /v1/runs/:id/spec` — returns the run's spec artifact with rendered markdown and parsed frontmatter. Policy resource: `run_spec`/`run_spec.read`. 404 if spec not found; 403 if cross-tenant. Response schema: `runSpecResponseSchema` (`{ artifact, markdown, frontmatter }`).
- `POST /v1/runs/:id/feedback` — creates artifact feedback for the run. Body schema: `createRunFeedbackRequestSchema` (`{ target: 'artifact', title, body, anchor? }`). Always creates with `status: 'open'`. Policy resource: `run_feedback`/`run_feedback.create`. Returns 201 with `Feedback`. Status lifecycle transitions (address/resolve/wont_fix/reopen) are NOT exposed here — no PATCH routes exist. **Anchor contracts:** `anchor` uses `artifact_range` kind with `from`/`to` fields that are zero-based Unicode codepoint offsets into the rendered Markdown string (use `[...str]` to count in JavaScript); `to` must be strictly greater than `from`. This is defined in `packages/api-contract/src/domain-value-objects.ts` `feedbackAnchorSchema`.
- `GET /v1/runs/:id/feedback` — lists all feedback for the run scoped to the caller's tenant. Policy resource: `run_feedback`/`run_feedback.list`. Returns 200 with `{ feedback: Feedback[] }`.
- `POST /v1/runs/:id/feedback/:feedbackId/thread` — appends a reply to an existing feedback item's thread without changing the feedback status. Policy resource: `run_feedback.thread.append`. Returns 200 with the updated `Feedback`. Body schema: `appendFeedbackThreadRequestSchema` (`{ body }`). Implemented in `packages/core/src/feedback-lifecycle.ts` (`appendFeedbackThreadReply`) and `packages/persistence/src/domain-repositories.ts` (`DrizzleFeedbackRepository.appendFeedbackThread`); exposed via `packages/sdk/src/client.ts` (`appendFeedbackThread(runId, feedbackId, request)`).

Failure reason test coverage: `packages/core/src/routes.spec.ts` covers GET/SSE serialization of matching failure reasons. `apps/control-plane/src/run-failure-reason.integration.spec.ts` drives create → auto-dispatch → provider-auth failure through production dispatch and asserts `provider_auth_failed` on GET and SSE plus no-leak sentinels.

Provider composition test seams:

- Core registry/composition behavior is covered by `packages/core/src/extension-registry.spec.ts` and `packages/core/src/provider-composition.spec.ts`.
- Control-plane startup composition is inspected through `ControlPlaneServerOptions.onProviderComposition`; production startup logs sanitized summaries through `logProviderCompositionDiagnostics`.

Runtime startup applies committed migrations with `migrateSqliteDatabase(database)`. Use the Drizzle Kit command only to generate future migrations from `packages/persistence/src/schema.ts`; do not require migration generation before running tests or the app.

## Package generation path

Use the built-in Nx JavaScript library generator. This concrete example creates an adapter-scoped package named `example-provider`; use the same command shape for each future package and change only the two shell variable values before running it:

```bash
PACKAGE_NAME=example-provider
PACKAGE_SCOPE=adapter
pnpm nx g @nx/js:library "packages/${PACKAGE_NAME}" \
  --bundler=tsc \
  --unitTestRunner=vitest \
  --linter=eslint \
  --importPath="@autocatalyst/${PACKAGE_NAME}" \
  --strict=true \
  --minimal=true
```

After generation, verify or add these concrete fields for the `example-provider` package in `packages/example-provider/project.json`:

```json
{
  "projectType": "library",
  "tags": ["type:lib", "scope:adapter"],
  "targets": {
    "build": {},
    "lint": {},
    "test": {}
  }
}
```

Use the established initial tags as examples:

- `api-contract`: `type:lib`, `scope:contract`
- `core`: `type:lib`, `scope:core`, `plane:control`
- `control-plane`: `type:app`, `scope:control-plane`, `plane:control`
- `execution`: `type:lib`, `scope:execution`, `plane:execution`
- `persistence`: `type:lib`, `scope:persistence`, `plane:control`
- `sdk`: `type:lib`, `scope:sdk`

## Boundary enforcement

- `@nx/enforce-module-boundaries` is active in `eslint.config.mjs` for TypeScript files.
- `no-restricted-imports` blocks `@autocatalyst/execution/src/*` and relative execution `src/*` patterns.
- `pnpm test:boundaries` lints two committed fixtures:
  - `tools/boundary-tests/fixtures/valid-control-plane-import.ts` must pass with `@autocatalyst/execution`.
  - `tools/boundary-tests/fixtures/invalid-execution-internal-import.ts` must fail with `no-restricted-imports`.
